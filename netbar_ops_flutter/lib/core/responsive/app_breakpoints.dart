class AppBreakpoints {
  /// Below this width, treat layout as "narrow" (phone/small tablet).
  static const double narrow = 900;

  /// Below this width, treat layout as "phone" for tighter spacing.
  static const double phone = 600;
}

